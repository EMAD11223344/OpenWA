version: '3.8'

services:
  neonize-gateway:
    build: .
    container_name: neonize-gateway
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DATA_DIR=/app/data
      - LOG_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: unless-stopped
